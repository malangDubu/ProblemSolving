/*
#include <iostream>
using namespace std;

int main()
{
    int N; cin >> N;

    int biggest, smallest;
    for (int i=0; i<N; i++) {
        int now; cin >> now;
        if (i==0) {
            biggest = now; smallest = now;
        }
        if(now > biggest) {
            biggest = now;
        }
        if(now < smallest) {
            smallest = now;
        }
    }

    cout << smallest << " "<< biggest ;
    return 0;
}
*/
